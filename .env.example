# MiniMax M2 API Configuration
#
# The MiniMax M2 API is compatible with the OpenAI SDK.
# You need to set your API Key and optionally the Base URL.

# 1. API Key (REQUIRED)
# Get your key from the MiniMax Developer Platform.
MINIMAX_API_KEY="YOUR_MINIMAX_API_KEY_HERE"

# 2. Base URL (OPTIONAL - only needed if the default changes or for a specific region)
# The default is the OpenAI-compatible endpoint for international users.
# Default: https://api.minimax.io/v1/chat/completions
# MINIMAX_BASE_URL="https://api.minimax.io/v1/chat/completions"
